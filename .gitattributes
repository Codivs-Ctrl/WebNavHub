# 统一行尾，避免跨平台协作时产生无意义的整文件差异
* text=auto

# shell 脚本在 Linux（含树莓派）上必须是 LF，否则会报 “\r”: command not found
*.sh text eol=lf

# Windows 下的 PowerShell 脚本保持 CRLF
*.ps1 text eol=crlf

# 二进制资源不做行尾转换
*.webp binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
